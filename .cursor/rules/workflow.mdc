---
description: 前端 AI 开发工作流规则 - 标准开发流程（BRAINSTORM → ANALYZE → [CONTRACT] → TDD → REVIEW → DOCUMENT）+ 工具节点（CONTEXT / DIGEST）
alwaysApply: true
---

# 前端 AI 开发工作流

## 启动规则

对话开始时，读取 `skills/using-fe-workflow/SKILL.md`，根据路由表判断当前处于哪个工作流节点并做出相应调度。

**Skill-First 原则**：在执行任何开发任务之前，先确定当前节点，加载对应 Skill。

## 工作流节点路由

| 用户意图 | 路由节点 | 读取文件 |
|---------|---------|---------|
| 收到模糊需求/想法 | BRAINSTORM | `skills/01-brainstorming/SKILL.md` |
| 收到 PRD 或 design.md 确认 | ANALYZE | `skills/02-requirements-analysis/SKILL.md` |
| 生成类型合约 / 锁定接口定义（可选） | CONTRACT | `skills/07-type-contract/SKILL.md` |
| 开始写组件/功能/工具函数 | TDD | `skills/03-tdd-cycle/SKILL.md` |
| 功能实现完成，需要检查 | REVIEW | `skills/04-code-review/SKILL.md` |
| CR 通过，需要文档沉淀 | DOCUMENT | `skills/05-documentation/SKILL.md` |
| 查看项目现状 / 查现有组件接口（随时） | CONTEXT | `skills/08-context/SKILL.md` |
| 做阶段总结 / 整合文档（随时） | DIGEST | `skills/09-digest/SKILL.md` |

## Hard Gate（强制检查）

**写任何实现代码之前，检查以下条件：**

- `docs/specs/` 下是否存在已确认的 design.md，**或**
- `docs/plans/` 下是否存在 tasks.md

如果两者均不存在：停止编码，触发 BRAINSTORM 节点。

## 工作流节点详情

### BRAINSTORM — 需求澄清

触发：`/viktor:think` 或检测到模糊需求
冷启动：扫描 `docs/specs/` 下已有文件；有文件时询问新建还是更新已有文档，选择更新则加载该文件直接进入修改模式

执行：
1. 读取 `skills/01-brainstorming/SKILL.md`
2. 识别关键决策点，一次性批量提出最多 3 个问题；可由上下文推断的直接采用并在假设中声明
3. 提出 2-3 个技术方案并对比
4. 生成并保存 `docs/specs/YYYY-MM-DD--<feature>.md`
5. 用户确认后提示进入 `/viktor:plan`

### ANALYZE — 需求分析

触发：`/viktor:plan` 或 BRAINSTORM 完成
冷启动：对话中无 BRAINSTORM 信号时，扫描 `docs/specs/` 供选择；已有 tasks.md 时询问覆盖或新建

执行：
1. 读取 `skills/02-requirements-analysis/SKILL.md`
2. 解析 design.md 或 PRD，提取功能点
3. 拆解为粒度合适的任务列表（每条 2-5 分钟，一个 TDD 循环）
4. 标注优先级（P0/P1/P2）和依赖关系
5. 保存 `docs/plans/YYYY-MM-DD--tasks.md`
6. 根据任务构成评估是否推荐 CONTRACT 节点，输出双路导航卡

### CONTRACT — 类型合约（可选）

触发：`/viktor:contract` 或 ANALYZE 推荐后用户选择
冷启动：对话中无 ANALYZE 信号时，扫描 tasks.md 供选择；已有合约文件时询问覆盖或追加

执行：
1. 读取 `skills/07-type-contract/SKILL.md`
2. 检查前置产物（tasks.md > design.md，两者均无则停止引导）
3. 提取并分组类型：实体 / Props / API / 状态 / 工具函数签名
4. 生成 `docs/contracts/YYYY-MM-DD--<feature>.types.ts`
5. 用户确认后 commit，提示进入 `/viktor:code`

### TDD — 测试驱动开发

触发：`/viktor:code`
冷启动：对话中无 ANALYZE 信号时，列出所有 tasks.md 及完成度；全部完成的加警告；用户选文件或选 N 重定向到 `think`

执行（每个任务循环）：
1. 读取 `skills/03-tdd-cycle/SKILL.md`
2. 从 tasks.md 取任务
3. RED：先写 Vitest 测试，运行确认失败
4. GREEN：写最少实现代码让测试通过
5. REFACTOR：重构，确认测试仍通过
6. `git commit -m "feat/fix: 描述"`
7. 循环直到所有任务完成
8. 提示进入 `/viktor:cr`

**强制规则**：没有红色的失败测试，禁止写实现代码。

### REVIEW — 代码审查

触发：`/viktor:cr`
冷启动：对话中无 TDD 完成信号时，检查 tasks.md 未完成任务数并警告；已有 review.md 时询问是否覆盖

执行：
1. 读取 `skills/04-code-review/SKILL.md`
2. 按六轴框架审查：正确性 / 可维护性 / 性能 / 安全性 / 测试质量 / 类型合约一致性
   - 轴六仅在 `docs/contracts/` 存在合约文件时执行
3. 标记问题：`[BLOCKING]` / `[SUGGESTED]` / `[NIT]`
4. 生成 `docs/reviews/YYYY-MM-DD--review.md`
5. 有 BLOCKING → 提示修复后返回 `/viktor:code`；无 BLOCKING → 提示 `/viktor:doc`

### INIT — 项目初始化

触发：`/viktor:init`
幂等行为：`project-context.md` 已存在时，询问"重新扫描"还是"仅补全缺失骨架"；重复执行安全

执行：
1. 读取 `skills/06-project-init/SKILL.md`
2. 扫描项目结构，生成 `docs/project-context.md`
3. 检查并生成活文档骨架（文件已存在则跳过）：
   `docs/component-catalog.md` / `docs/api-catalog.md` / `docs/architecture.md` / `docs/adrs/README.md`

### DOCUMENT — 文档沉淀

触发：`/viktor:doc`（前提：review 通过）
冷启动：对话中无 REVIEW PASS 信号时，扫描 `docs/reviews/` 供选择；含 BLOCKING 时警告并确认

执行：
1. 读取 `skills/05-documentation/SKILL.md`
2. 汇总产物；检测 `skills/` / `commands/` 变更，若有则提示同步三端入口
3. 自动编号（ADR-001 格式）；询问是否替代历史 ADR；生成 ADR 文档
4. 条件更新活文档（component-catalog / api-catalog / architecture / adrs/README）
5. 更新 `CHANGELOG.md`
6. 完成后在导航卡中固定提供 `/viktor:digest` 非阻塞选项（无条件触发，不依赖 ADR 数量）

### CONTEXT — 项目快照（工具节点，随时可用）

触发：`/viktor:context`（无前置条件）

执行：
1. 读取 `skills/08-context/SKILL.md`
2. 检查 5 个活文档是否存在，格式化输出到对话
3. 不生成文件，不创建 commit

**集成**：BRAINSTORM 开始时，若 project-context.md 存在，非阻塞提示用户可执行本命令

### DIGEST — 文档整合（工具节点，随时可用）

触发：`/viktor:digest`（无前置条件，或响应 DOCUMENT 建议）

执行：
1. 读取 `skills/09-digest/SKILL.md`
2. 扫描并读取 docs/ 下所有文档
3. 生成 `docs/digest/YYYY-MM-DD--digest.md`（5 个章节），commit

## 产物目录规范

```
docs/
├── project-context.md      # /viktor:init 产物（知识地图）
├── component-catalog.md    # /viktor:init 初始化，/viktor:doc 持续更新
├── api-catalog.md          # /viktor:init 初始化，/viktor:doc 持续更新
├── architecture.md         # /viktor:init 初始化，/viktor:doc 持续更新
├── specs/                  # design.md（/viktor:think 产物）
├── plans/                  # tasks.md（/viktor:plan 产物）
├── contracts/              # types.ts（/viktor:contract 产物，可选）
├── reviews/                # review.md（/viktor:cr 产物）
├── adrs/                   # adr.md（/viktor:doc 产物）
│   └── README.md           # ADR 索引，/viktor:doc 持续更新
└── digest/                 # digest.md（/viktor:digest 产物）
```

## 技术栈

由 `docs/project-context.md` 提供（执行 `/viktor:init` 后自动生成，框架无关）。

React/Next.js 参考实现见 `references/react-nextjs-conventions.md`。
